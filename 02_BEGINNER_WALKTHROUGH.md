# 처음부터 끝까지 따라하기 (비전공자용)

이 문서 하나만 순서대로 따라오시면 매일 자동으로 "오늘의 논문 초안"이 만들어지는 상태까지 세팅됩니다. 소요 시간은 약 20~30분이고, 코드를 직접 만질 필요는 없습니다 — 복사/붙여넣기와 클릭만 하시면 됩니다.

먼저 전체 그림을 한 문장으로: **GitHub**(코드를 올려두고 자동으로 실행해주는 무료 서비스)에 지금 받으신 코드를 올려두면, 매일 정해진 시각에 자동으로 논문을 고르고 이미지를 만들어서 "Issue"(할 일 게시판 같은 것)에 결과물을 올려줍니다. 그러면 그걸 보고 직접 Instagram에 올리시면 됩니다.

---

## 0단계. 준비물

- 받으신 `ig-science-agent.zip` 파일을 컴퓨터에서 압축 해제해두세요 (더블클릭하면 보통 자동으로 풀립니다).
- Anthropic 계정 (console.anthropic.com) — 없으면 1단계에서 같이 만듭니다.

## 1단계. GitHub 계정 만들기

1. [github.com](https://github.com) 접속 → 우측 상단 **Sign up** 클릭
2. 이메일, 비밀번호, 사용자 이름(아무거나 가능, 예: `chiyeol-science`)을 입력하고 가입 절차를 따라갑니다 (무료입니다).
3. 가입 후 로그인된 상태로 다음 단계로 넘어갑니다.

## 2단계. 새 저장소(Repository) 만들기

"저장소"는 코드를 담아두는 폴더라고 생각하시면 됩니다.

1. 로그인 후 우측 상단 **+** 버튼 → **New repository** 클릭
2. Repository name에 원하는 이름 입력 (예: `science-ig-agent`)
3. **Public** 선택 (Private는 나중에 4단계의 무료 이미지 호스팅이 안 됩니다 — 코드 안에는 개인정보나 비밀번호가 없으니 공개해도 안전합니다. 실제 비밀 값은 뒤에서 별도의 "Secrets"라는 안전한 곳에 저장합니다.)
4. 다른 옵션은 그대로 두고 **Create repository** 클릭

## 3단계. 코드 파일 올리기

방금 만든 저장소 페이지에서:

1. **Add file** 버튼(또는 **uploading an existing file** 링크) → **Upload files** 클릭
2. 압축 해제한 `ig-science-agent` 폴더 안의 아래 항목들을 **폴더째로** 끌어다(드래그) 업로드 화면에 놓습니다:
   - `scripts` 폴더 전체
   - `templates` 폴더 전체
   - `run_daily.py`
   - `requirements.txt`
   - `00_SETUP_GUIDE.md`, `01_COPYRIGHT_POLICY.md`, `05_SELECTION_CHECKLIST.md`, `README.md` (원하면 다 올려도 되고, 나중에 참고용이라 안 올려도 자동화 자체는 작동합니다)
3. 화면 하단에 **Commit changes** 버튼 클릭해서 저장

   > 폴더를 드래그할 때는 폴더 안을 열어서 파일 하나하나를 끄는 게 아니라, **폴더 아이콘 자체**를 통째로 끌어다 놓으셔야 폴더 구조가 그대로 유지됩니다.

4. `.github` 폴더는 이름이 점(.)으로 시작해서 컴퓨터에 따라 안 보이거나 드래그가 잘 안 될 수 있습니다. 이건 아래 별도 방법으로 만듭니다.

### 3-1. 자동 실행 설정 파일 만들기 (복사/붙여넣기)

1. 저장소 메인 페이지에서 **Add file** → **Create new file** 클릭
2. 파일 이름 입력칸에 아래를 정확히 입력합니다 (슬래시를 입력하면 자동으로 폴더가 만들어집니다):
   ```
   .github/workflows/daily_post.yml
   ```
3. 그 아래 큰 편집창에, 받으신 zip 안의 `.github/workflows/daily_post.yml` 파일을 열어서 **내용 전체를 복사**해 붙여넣으세요. (메모장/텍스트 편집기로 그 파일을 열면 내용을 복사할 수 있습니다.)
4. 하단 **Commit changes** 클릭

이제 저장소에 필요한 파일이 다 올라갔습니다.

## 4단계. GitHub Pages 켜기 (이미지를 공개적으로 보여주기 위한 설정)

1. 저장소 페이지 상단 **Settings** 탭 클릭
2. 왼쪽 메뉴에서 **Pages** 클릭
3. **Build and deployment** → Source를 **Deploy from a branch**로
4. Branch를 **main**, 폴더를 **/docs**로 선택 → **Save**

   > `/docs` 폴더는 아직 저장소에 없어서 목록에 안 보일 수도 있어요. 그래도 그냥 `main` / `/docs`로 저장해두시면, 나중에 6단계에서 자동 실행이 한 번 돌고 나면 자동으로 인식됩니다.

## 5단계. Anthropic API 키 발급받기

이건 "논문이 왜 중요한지" 분석하는 AI를 호출할 때 필요한 열쇠입니다.

1. [console.anthropic.com](https://console.anthropic.com) 접속 → 로그인/가입
2. 왼쪽 메뉴 **API Keys** → **Create Key** 클릭 → 이름 아무거나 입력 후 생성
3. 생성된 키(`sk-ant-...`로 시작하는 긴 문자열)를 복사해두세요 — 이 화면을 벗어나면 다시 볼 수 없으니 메모장에 잠깐 붙여넣어두시는 걸 추천합니다.
4. **결제 관련 안내**: 이 API는 사용한 만큼 과금되는 유료 서비스입니다 (신용카드 등록 필요). 다만 하루 논문 1편 분석에 드는 비용은 아주 적습니다(보통 건당 몇 센트 수준). Billing 메뉴에서 카드를 등록하고, 원하시면 월 한도(spending limit)를 낮게 설정해두시면 예상 밖의 과금을 막을 수 있습니다.

## 6단계. GitHub에 비밀 값(Secrets) 등록하기

API 키처럼 남에게 보이면 안 되는 값은 코드에 직접 쓰지 않고 GitHub의 잠긴 보관함에 따로 저장합니다.

1. 저장소 **Settings** → 왼쪽 메뉴 **Secrets and variables** → **Actions** 클릭
2. **New repository secret** 클릭
3. 아래 두 개를 각각 등록합니다 (이름을 정확히 똑같이 입력해야 합니다):
   - Name: `ANTHROPIC_API_KEY` / Secret: 5단계에서 복사해둔 키
   - Name: `CROSSREF_MAILTO` / Secret: 본인 이메일 주소 (예: cychoi0701@gmail.com — 이건 비밀값이 아니라 논문 데이터베이스 API 예의상 요구하는 이메일이라, 여기 등록하지 않고 코드에 직접 써도 되지만 편의상 여기 등록해도 무방합니다)

## 7단계. 첫 실행 테스트해보기

1. 저장소 상단 **Actions** 탭 클릭
2. 왼쪽에 **Daily science content draft**라는 항목이 보이면 클릭
3. 오른쪽의 **Run workflow** 버튼(초록색) → 다시 한 번 **Run workflow** 클릭
4. 페이지를 새로고침하면 실행 중인 항목이 뜹니다. 노란 점(진행중) → 초록 체크(성공) 또는 빨간 X(실패)로 바뀝니다. 보통 1~3분 걸립니다.
5. 만약 빨간 X가 뜨면, 그 항목을 클릭해서 어느 단계에서 실패했는지 로그를 볼 수 있습니다 (그 내용을 저에게 붙여넣어 주시면 같이 고칠 수 있어요).

---

## GitHub Issue에 올라온 결과물 확인하는 방법

이게 질문하신 부분이에요. 매일 논문을 3편(기본값) 다루기 때문에 Issue도 하루에 3개씩 올라옵니다. 실행이 성공하면:

1. 저장소 상단 메뉴에서 **Issues** 탭을 클릭합니다 (Code, Issues, Pull requests 등이 나열된 줄에 있습니다).
2. **"Draft IG post 1/3 - 2026-08-XX (Cell)"**, **"...2/3... (Nature)"**, **"...3/3... (Science)"** 처럼 번호와 저널명이 붙은 새 글 3개가 보일 거예요. 하나씩 클릭해서 엽니다.
3. 각 Issue 안에 그 논문의 캐러셀 이미지 4~5장이 실제 그림으로 표시되고, 그 아래에 캡션(게시글 문구) 텍스트가 코드 박스 안에 들어있습니다.
4. 이미지 하나하나를 마우스 오른쪽 클릭 → **이미지 저장** (또는 이미지를 클릭해서 크게 연 다음 다운로드)으로 컴퓨터/휴대폰에 저장합니다.
5. 캡션 박스 안의 텍스트를 전체 선택해서 복사해둡니다.
6. Instagram 앱을 열어 평소처럼 게시물 만들기 → 저장한 이미지 여러 장을 순서대로 선택(캐러셀) → 다음 → 복사해둔 캡션을 붙여넣기 → 게시.
7. 나머지 2개의 Issue도 같은 방식으로 반복합니다. 하루 3번에 나눠 올리셔도 되고, 한 번에 몰아 올리셔도 됩니다.
8. 다 확인해서 게시까지 마친 Issue는 오른쪽의 **Close issue** 버튼을 눌러 닫아두면(선택사항) 나중에 뭐가 남았는지 헷갈리지 않습니다.

매일 이 워크플로가 자동으로 돌면서 Issues 탭에 새 글이 3개씩 쌓입니다. 혹시 이미지 2번 자리(원본 논문 스크린샷)가 비어있다는 안내가 Issue에 적혀있다면, `01_COPYRIGHT_POLICY.md` 기준에 따라 직접 논문 페이지 스크린샷을 찍어서 캐러셀에 끼워넣어야 하는 경우입니다 (저작권상 자동으로 채울 수 없는 논문일 때 나오는 안전장치예요).

> 하루 편수를 3편에서 다른 숫자로 바꾸고 싶으면, Actions 탭 → `Daily science content draft` → **Run workflow** 버튼을 누를 때 나오는 `post_count` 입력칸에 원하는 숫자를 넣고 수동 실행하면 그 실행만 그 편수로 돕니다. 매일 자동으로 도는 기본값 자체를 바꾸려면 `.github/workflows/daily_post.yml`의 `default: "3"` 부분을 원하는 숫자로 고치면 됩니다.

---

## 막히면

- Actions 탭에서 빨간 X가 뜨는데 원인을 모르겠다 → 실패한 실행을 클릭해서 나오는 로그 화면을 캡처하거나 복사해서 저에게 보여주세요.
- Secrets 이름을 잘못 입력한 것 같다 → Settings → Secrets and variables → Actions에서 삭제 후 다시 등록하면 됩니다.
- 매일 실행되는 시각을 바꾸고 싶다 → `.github/workflows/daily_post.yml` 파일 맨 위쪽 `cron: "0 13 * * *"` 부분을 수정하면 되는데, 이 값은 UTC(세계표준시) 기준이라 원하시는 시각을 알려주시면 제가 정확한 값을 계산해드릴게요.
